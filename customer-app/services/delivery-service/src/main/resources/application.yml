server:
  port: ${DELIVERY_SERVICE_PORT:8080}

spring:
  application:
    name: delivery-service

logging:
  pattern:
    console: '{"timestamp":"%d","level":"%p","service":"delivery-service","message":"%m"}%n'
