{{- if .Values.incidentService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.incidentService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.incidentService.name }}
    app.kubernetes.io/name: {{ .Values.incidentService.name }}
    app.kubernetes.io/part-of: itsm-app
    environment: {{ .Values.global.env }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.incidentService.name }}
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
{{- end }}
